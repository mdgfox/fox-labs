import { StoryObj } from '@storybook/react';
import { Menu3D } from '../Components/Menu/Menu3D';

const meta = {
    title: 'Kitsune/Menu 3D',
    component: Menu3D,
};

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {};
